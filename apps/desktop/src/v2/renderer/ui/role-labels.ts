/**
 * Public UI vocabulary for the agent role that coordinates a workspace team. The value lives in the
 * domain package so the renderer and the main process cannot drift apart on what the user is shown.
 */
export { ORCHESTRATOR_LABEL } from "@forgedeck/compazio-v2-domain";
